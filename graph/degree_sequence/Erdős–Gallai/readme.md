차수열이 주어졌을 때, 실제 가능한 그래프인지 판별하는 내용.
누적 합을 이용하여 정렬을 딱 한번만 하는 트릭이다. 따라서 시간 복잡도는 $O(n \log n)$이다. 여기서 정렬이 가장 오래걸리기 때문에 $n$의 값이 크다면 Counting sort를 이용해서 $O(n)$으로 판별할 수 있다.
하지만 이 방식으로는 판별만 가능하고 실제로 구성을 하지는 못한다.

이게 가능한 이유는 아래에 설명
